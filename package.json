{
  "name": "medquiz-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r android/app/src/main/assets/* dist/",
    "dev": "node server.js",
    "start": "node server.js"
  }
}
